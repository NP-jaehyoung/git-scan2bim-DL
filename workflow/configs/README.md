# Workflow Configs

This folder is reserved for future runtime configuration files for the executable Scan2BIM pipeline.

Likely contents later:
- inference presets
- label-space presets
- export thresholds
- Revit import defaults

For now, command-line options are defined directly in the workflow scripts under `workflow/scripts/`.
