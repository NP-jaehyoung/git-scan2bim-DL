# Revit 2024 Session

`active_import.json` is generated by:

```bash
python workflow/revit2024/prepare_revit2024_import.py --input <candidate_json> --units meters --write-session
```

The Revit 2024 pyRevit buttons read this file first. If it does not exist, they fall back to a file-open dialog.
